import { useState } from 'react';import { Link } from 'react-router-dom';import { useCart } from '../context/CartContext';import { useToast } from '../context/ToastContext';import './ProductCard.css';export default function ProductCard({ product, badge }) {  const [saved, setSaved] = useState(false);  const { addItem } = useCart();  const { addToast } = useToast();  return (    <div className="product-card">      <Link to={`/products/${product.slug}`} className="product-card-image">        <img src={product.image} alt={`${product.name} gift hamper`} loading="lazy" />        {badge && <span className="product-badge">{badge}</span>}        <button className="product-add-cart" onClick={(e) => { e.preventDefault(); addItem(product); addToast(`${product.name} added to cart!`); }} aria-label="Add to cart">          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2">            <line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/>          </svg>        </button>      </Link>      <div className="product-card-info">        <Link to={`/products/${product.slug}`} className="product-card-name">{product.name}</Link>        <div className="product-card-bottom">          <span className="product-card-price">₹{product.price.toFixed(2)}</span>          <button            className={`save-later-btn ${saved ? 'saved' : ''}`}            onClick={(e) => { e.preventDefault(); setSaved(!saved); }}            aria-label={saved ? 'Remove from saved' : 'Save for later'}          >            <svg width="16" height="16" viewBox="0 0 24 24" fill={saved ? '#FF6B9D' : 'none'} stroke="#FF6B9D" strokeWidth="2">              <path d="M19 21l-7-5-7 5V5a2 2 0 0 1 2-2h10a2 2 0 0 1 2 2z"/>            </svg>          </button>        </div>      </div>    </div>  );}